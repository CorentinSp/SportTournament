#ifndef MATCH_HPP
#define MATCH_HPP

#include <QObject>

class Match : public QObject
{
    Q_OBJECT
public:
    explicit Match(QObject *parent = nullptr);

signals:

public slots:
};

#endif // MATCH_HPP